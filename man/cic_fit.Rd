% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cic.R
\name{cic_fit}
\alias{cic_fit}
\title{Fit Changes-in-Changes Inference}
\usage{
cic_fit(
  Y11,
  Y10,
  Y01,
  Y00,
  method = c("no-split", "split", "ai", "bse", "bpc"),
  ...,
  B = 1000,
  epsilon_n = NULL,
  level = 0.95,
  timings = FALSE
)
}
\arguments{
\item{Y11}{Numeric vector: treated post-period sample.}

\item{Y10}{Numeric vector: treated pre-period sample.}

\item{Y01}{Numeric vector: control post-period sample.}

\item{Y00}{Numeric vector: control pre-period sample.}

\item{method}{Character vector. One or more of \code{"no-split"},
\code{"split"}, \code{"ai"}, \code{"bse"}, or \code{"bpc"}.}

\item{...}{Reserved for future extensions. Unused arguments are rejected.}

\item{B}{Integer: number of bootstrap replications. Only used for
\code{"bse"} and \code{"bpc"}.}

\item{epsilon_n}{Numeric scalar or function. Bandwidth multiplier for the
split and no-split density estimators, where
\eqn{h_{n_2}(u) = \epsilon_{n_2} u(1-u)}. The default is
\eqn{\epsilon_{n_2} = 1 / \log(n_2)}.}

\item{level}{Numeric confidence level.}

\item{timings}{Logical. If \code{TRUE}, print elapsed time after each major
computation block.}
}
\value{
An S3 object of class \code{c("quantcdf_cic_fit", "quantcdf_fit")}.
}
\description{
Estimates the Changes-in-Changes contrast
\eqn{E[Y_{11}] - E[F_{Y_{01}}^{-1}(F_{Y_{00}}(Y_{10}))]}.
The counterfactual component is obtained from \code{\link{fit}} rather than
duplicated.
}
\examples{
set.seed(42)
Y11 <- rnorm(200, 1)
Y10 <- rnorm(200)
Y01 <- rnorm(200)
Y00 <- rnorm(200)
cic_fit(Y11, Y10, Y01, Y00, method = "no-split")

}
